#!/usr/bin/node
module.exports = {
  /**
   * sum - Sum of 2 numbers.
   * @param {Number} x - The first number.
   * @param {Number} y - The second number.
   *
   * @returns The sum of the 2 numbers.
   */
  add: (x, y) => x + y
};
